# Agent Rules

## Explicit-request-only actions

Like commits and pushes, **changelog edits (`public/changelog/*.md`) are only
made on explicit user request** — never proactively alongside feature work.
